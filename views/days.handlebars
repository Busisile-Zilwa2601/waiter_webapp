<div calss='row'>
    <div class=''>
        <h1>Booked Days</h1>
        <hr />
    </div>
</div>
<div class="row">
        {{#each week_days}}
        <div class='two columns '>
            <div class='{{color}}'>{{day}}</div>
            <hr />
            {{#each waiters}}
            <h6 class='caps'><i class="far fa-user"></i> {{names}}</h6>
            {{/each}}
        </div>
        {{/each}}
</div>
<hr />
<div class='row'>
    <form action='/reset' method="post">
        <button type="submit" name="button" class="btn btn-primary">Reset</button>
    </form>
</div>